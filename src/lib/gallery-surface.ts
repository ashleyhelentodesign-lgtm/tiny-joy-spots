/** White field + dot grid behind gallery cards (home, profile “My spots”, tag map). */
export const GALLERY_SURFACE_CLASS = "w-full bg-white";

export const GALLERY_SURFACE_STYLE = {
  backgroundImage:
    "radial-gradient(circle at center, #ded8d2 1.25px, transparent 1.25px)",
  backgroundSize: "22px 22px",
} as const;
